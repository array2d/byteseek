// python·run(code) -> out：跑一段 python3 -c，返回 stdout（@ 句柄，读时兑现）。

lib python {
	rwfunc run(code:[]char/utf32) -> (out:[]char/utf32) {
		println("🔧 python:", code)
		noenv:[int64]·[]char/utf32 = {}
		a:[int64]·[]char/utf32 = {"python3", "-c", code}
		networld/proc·exec(a, noenv) -> ret, o, e
		println("↳", o, e)
		o -> out
	}
}
